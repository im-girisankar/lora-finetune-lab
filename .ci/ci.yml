# CI configuration — parked / placeholder
#
# This file is a placeholder for a future GitHub Actions (or similar) workflow.
# Uncomment and adapt when you are ready to add CI.
#
# name: CI
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         python-version: ["3.10", "3.11", "3.12"]
#
#     steps:
#       - uses: actions/checkout@v4
#
#       - name: Set up Python ${{ matrix.python-version }}
#         uses: actions/setup-python@v5
#         with:
#           python-version: ${{ matrix.python-version }}
#
#       - name: Install dev dependencies
#         run: pip install -e ".[dev]"
#
#       - name: Lint with ruff
#         run: ruff check src tests
#
#       - name: Run tests
#         run: pytest
